numpy>=1.24,<3
sounddevice>=0.4.6,<0.6
elevenlabs>=1.50,<3
websockets>=13
python-dotenv>=1.0,<2

# --- Cerebro y voz de Jarvis (todo esto es gratis) -------------------------
groq>=0.11,<1                # cerebro (Llama vía Groq, gratis)
requests>=2.31,<3            # clima (Open-Meteo)
ddgs>=9,<10                  # búsqueda en internet, sin clave
fpdf2>=2.7,<3                # generar informes en PDF
python-docx>=1.1,<2          # generar informes en Word (.docx)
openpyxl>=3.1,<4              # generar informes en Excel (.xlsx)
SpeechRecognition>=3.10,<4   # transcribir voz a texto (Google, sin clave)
edge-tts>=6.1,<8             # voz de respuesta neuronal (gratis, sin clave)
playsound<1.3                # reproducir la voz de Edge TTS (mp3)
pyttsx3>=2.90,<3             # voz de respaldo offline si no hay internet
pyperclip>=1.8,<2            # leer el portapapeles
flask>=3.0,<4                # servidor del dashboard
psutil>=5.9,<8                # estadísticas del sistema para el dashboard

# --- Solo Windows (control de volumen) -------------------------------------
pycaw>=20230407; sys_platform == "win32"
comtypes>=1.2; sys_platform == "win32"
